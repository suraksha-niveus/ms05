apiVersion: v1
kind: Service
metadata:
  name: {{ include "gradle-sample-app.fullname" . }}
  labels:
    {{- include "gradle-sample-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gradle-sample-app.selectorLabels" . | nindent 4 }}
